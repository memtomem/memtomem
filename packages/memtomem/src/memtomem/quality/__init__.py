"""Memory Quality Lab (#1798): local search-evaluation building blocks.

This package holds the runtime-importable pieces of the Quality Lab —
pure-function IR metrics (:mod:`memtomem.quality.metrics`) and the profile /
corpus / index fingerprints (:mod:`memtomem.quality.fingerprints`) that gate
replay-report comparison. Replay and comparison land in later slices of #1802.
"""
