# B.2 v2 — Phase 1 validation report

Measurement results for the Phase 1 methodology check: does genre
rotation (runbook / postmortem / adr / troubleshooting) produce
style-distinct chunks that BM25 and/or dense can separate, when
sharing the same topic?

**Corpus under test**: `caching` topic × `ko` language. 4 genres ×
4 chunks = 16 chunks. Generated by Gemini with closed-set subtopic
vocabulary and strict genre style constraints (except
troubleshooting, which used an earlier looser prompt — see caveat
§ 5).

## 1. Dense embedding separation (cosine distance)

Model: `sentence-transformers/paraphrase-multilingual-MiniLM-L12-v2`
(384d). Each chunk embedded independently; pairwise cosine distance
(`1 - cos_sim`) computed within and across genres.

### Within-genre mean cosine distance (lower = more homogeneous)

| Genre | Mean distance | n pairs |
|---|---|---|
| adr | 0.3436 | 12 |
| postmortem | 0.2812 | 12 |
| runbook | 0.3062 | 12 |
| troubleshooting | 0.4184 | 12 |

### Across-genre mean cosine distance (higher = more separated)

| Genre pair | Mean distance | n pairs |
|---|---|---|
| adr ↔ postmortem | 0.3091 | 32 |
| adr ↔ runbook | 0.3216 | 32 |
| adr ↔ troubleshooting | 0.3538 | 32 |
| postmortem ↔ runbook | 0.2803 | 32 |
| postmortem ↔ troubleshooting | 0.3306 | 32 |
| runbook ↔ troubleshooting | 0.3377 | 32 |

### Aggregate

- within-genre mean: **0.3374**
- across-genre mean: **0.3222**
- ratio: **0.955** (inverted — across is *closer* than within)

**Verdict**: dense embeddings do not separate genres in this
corpus. Topic (caching/Redis) dominates the vector; tense and style
are not strong enough signals in a 384d general-purpose model on
short Korean technical text. This is **expected behavior for
general-purpose encoders** and not a methodology failure — just a
reframing of what we can detect.

## 2. BM25 lexical separation (Jaccard on token sets)

Tokenizer: `kiwipiepy` (Korean morphological analyzer). Token sets
per chunk derived from `tokenize_for_fts` output, lowercased,
min-length 2 chars.

### Inter-genre Jaccard (between-genre pair mean)

| Genre pair | Jaccard | Shared / Total |
|---|---|---|
| adr ↔ postmortem | 0.152 | 43 / 283 |
| adr ↔ runbook | 0.164 | 45 / 274 |
| adr ↔ troubleshooting | 0.174 | 61 / 350 |
| postmortem ↔ runbook | 0.207 | 57 / 276 |
| postmortem ↔ troubleshooting | 0.155 | 57 / 368 |
| runbook ↔ troubleshooting | 0.208 | 72 / 346 |

### Intra-genre Jaccard (within-genre pair mean)

| Genre | Mean | Range | n pairs |
|---|---|---|---|
| adr | 0.130 | [0.093, 0.149] | 6 |
| postmortem | 0.144 | [0.120, 0.200] | 6 |
| runbook | 0.097 | [0.059, 0.145] | 6 |
| troubleshooting | 0.126 | [0.092, 0.179] | 6 |

### Verdict: separation is NOT confirmed at Jaccard level

**Intra (0.10-0.14) is lower than Inter (0.15-0.21).** Within a
genre, any two chunks share *less* vocabulary than two chunks across
genres. This is because each genre batch covers 4 different primary
subtopics (redis / eviction / invalidation / stampede / replication)
whose subtopic-specific vocabulary dominates whatever genre marker
vocabulary exists.

Raw Jaccard ignores IDF. Genre markers that appear 4/4 in one genre
and 0/4 in others are swamped by the larger subtopic vocabulary
when measured as set overlap.

## 3. Genre marker tokens (high-IDF anchors)

Tokens that appear *only* in one genre (absent from all other
genres' token sets), with per-chunk frequency:

### `adr` (decision vocabulary)

`대신` (4), `지만` (3), `결정` (3), `방식` (3), `단순` (2),
`off` (2), `채택` (2), `관리` (2), `trade` (2), `아니` (2),
`선택` (2), `평가` (2)

### `postmortem` (incident narrative)

`후속` (4), `조치` (4), `KST` (4), `원인` (3), `고정` (2),
`강화` (2), `업데이트` (2), `수동` (2), `거부` (1),
`알람` (1), `평소` (1), `수정` (1)

### `runbook` (imperative procedures)

`ᆫ지` (3 — negative form marker), `SET` (2), `human` (1),
`절차` (1), `peak` (1), `접속` (1), `gb` (1), `상한` (1),
`CONFIG` (1), `다음` (1), `miss` (1), `목록` (1)

### `troubleshooting` (diagnostic)

`ᆸ니다` (4 — polite form), `어야` (4), `세요` (3), `만약` (3),
`대하` (2), `라면` (2), `응답` (2), `요청` (2), `먼저` (2),
`습니다` (2), `OS` (2), `증상` (2)

### Verdict: genre markers exist, just IDF-gated

The 4 most reliable markers per genre (4/4 or 3/4 presence) are
strong candidates for anchor vocabulary in Phase 4 query design.
See `b2-v2-query-portfolio.md` § "REQUIRED (Phase 4): genre-primary
queries".

## 4. Tense marker distribution

Manual check for Korean verb tense/mood markers:

| Marker family | adr | postmortem | runbook | troubleshooting |
|---|---|---|---|---|
| imperative (`~한다`, `~하라`) | 1/4 | 4/4 | 3/4 | 4/4 |
| past narrative (`~했다`, `~됐다`) | 2/4 | 4/4 | 4/4 | 4/4 |
| decision (`채택`, `선택했다`) | **4/4** | 1/4 | 0/4 | 1/4 |
| diagnostic (`의심`, `점검해`) | 1/4 | 4/4 | 3/4 | 4/4 |

**Only `adr` has a clean, genre-exclusive tense separation** (4/4
decision markers, ≤ 1/4 in all other genres). The other three
genres overlap heavily — every one uses imperative present and past
narrative. Separation there comes from vocabulary (SET/CONFIG for
runbook, KST/후속 for postmortem, 증상/의심 for troubleshooting),
not tense.

## 5. Chunk length distribution

| Genre | Mean chars | Range |
|---|---|---|
| adr | ~200 | ~180-210 |
| postmortem | 249 | 237-270 |
| runbook | 224 | 213-236 |
| troubleshooting | 438 | 408-468 |

`troubleshooting` is notably longer than the other three because it
was generated in an earlier Gemini session before the strict
150-400 char constraint was added to the prompt template. The
other three batches respected the limit.

**Caveat for the Phase 1 finding**: ADR is NOT longer than the
other compact genres, so the hypothesis "long ADR gets more dense
response because longer texts surface structure" cannot be tested
here. To test, ADR would need to be regenerated at 400-600 chars.
Deferred — see "Open questions" below.

## 6. Implications for v2 methodology

### What was validated

- **Dense/BM25 asymmetry exists**: dense is topic-dominant,
  BM25-IDF can pick up genre markers. This asymmetry is the source
  of `rrf_weights` sensitivity that MVP lacked.
- **Strong per-genre anchor vocabulary exists** (4/4 markers for
  `후속`, `조치`, `KST`, `대신`, `결정`, `채택`, `trade`-off): query
  designer can rely on these.
- **ADR tense is cleanly separable**; other genre distinctions rest
  on vocabulary.

### What was NOT validated (or invalidated)

- **Jaccard-level genre separation**: intra < inter. Genre rotation
  alone does not produce vocabulary-homogeneous clusters. Subtopic
  diversity dominates.
- **Dense embedding response to genre**: null result on this
  sample. General-purpose encoders on short Korean technical text
  cluster by topic, not style.

### Concrete corrections to the v2 plan

1. **Genre-primary queries promoted to core axis** (not memo, not
   deferred). `b2-v2-query-portfolio.md` section
   "REQUIRED (Phase 4)" spec bumps portfolio from 80 to 100 queries
   (10 genre-primary per language).
2. **Sensitivity assertion**: `[1,0]` vs `[0,1]` `rrf_weights` on
   genre-primary queries must produce different top-K. Early
   warning gate after Phase 2b (first topic complete) to avoid
   wasted generation if the anchor mechanism doesn't work.
3. **Subtopic diversity is a first-class signal source**, on par
   with genre rotation. Topic × subtopic × genre three-axis
   variation is what makes v2 differ from MVP, not genre rotation
   alone.

### Open questions (deferred to Phase 4-5)

- Does dense start responding to genre at longer chunk lengths
  (e.g., ADR 400-600 chars)? Would require a small follow-up batch
  with extended chunks.
- Does the same finding hold for English? Phase 1 was ko-only.
  Phase 2 will generate EN chunks; re-run intra/inter Jaccard on
  the first EN batch to confirm.
- Would a KO-specific embedding model (e.g., a BGE Korean variant)
  show dense genre response? Not in v2 scope; would require model
  swap.

## 7. Go/no-go call

**Go on Phase 2**, with the three corrections above baked into the
plan. Genre rotation is retained as orthogonal signal source
(subtopic-level vocabulary is the dominant one), and genre-primary
queries become the enforcement mechanism for the rrf_weights
sensitivity contract.

If Phase 2b's cross-rrf_weights sensitivity assertion fails (same
top-K under [1,0] and [0,1] on genre-primary queries), pause and
investigate before doing the remaining 14 topics.
